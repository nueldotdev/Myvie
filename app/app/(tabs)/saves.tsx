import { View, Text } from 'react-native'
import React from 'react'
import { AppText } from '@/components/app/AppText'

const Saves = () => {
  return (
    <View>
      <AppText style={{color: '#fff'}}>Saves</AppText>
    </View>
  )
}

export default Saves